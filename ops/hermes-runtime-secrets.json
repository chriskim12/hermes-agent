{
  "schema": "hermes-runtime-secrets/v1",
  "project": "hermes",
  "description": "Runtime secret governance manifest for the Hermes installation operated by Chris. Values must never be stored here.",
  "entries": [
    {
      "key": "HERMES_GATEWAY_TOKEN",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": true },
      "notes": "Gateway authentication token for this Hermes runtime."
    },
    {
      "key": "DATABASE_URL",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": true },
      "notes": "Hermes local/runtime database connection string."
    },
    {
      "key": "DISCORD_BOT_TOKEN",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": true },
      "notes": "Discord gateway bot token for this Hermes deployment."
    },
    {
      "key": "FISH_AUDIO_API_KEY",
      "classification": "shared-provider-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": false },
      "notes": "Provider credential consumed by Hermes TTS when configured; projected here when this runtime uses Fish Audio."
    },
    {
      "key": "LINEAR_API_KEY",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": true },
      "notes": "Linear API credential used by Hermes task operations and closeout guards."
    },
    {
      "key": "RAILWAY_API_TOKEN",
      "classification": "operator-provider-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": false },
      "notes": "Operator token used by Hermes for Railway-facing operations; projected only on operator hosts."
    },
    {
      "key": "SUPABASE_ACCESS_TOKEN",
      "classification": "operator-provider-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": false },
      "notes": "Operator token used by Hermes for Supabase-facing operations; projected only on operator hosts."
    },
    {
      "key": "WEBHOOK_SECRET",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": true },
      "notes": "Global webhook HMAC fallback secret for this Hermes gateway."
    },
    {
      "key": "HERMES_SSH_MACBOOK_PRIVATE_OPENSSH_B64",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": false },
      "notes": "SSH material already scoped to Hermes BWS project; projected only when SSH automation needs it."
    },
    {
      "key": "HERMES_SSH_RISU_PRIVATE_OPENSSH_B64",
      "classification": "hermes-owned-secret",
      "sourceOfTruth": { "type": "bws", "project": "hermes" },
      "runtimeProjection": { "envFile": "~/.hermes/.env", "required": false },
      "notes": "SSH material already scoped to Hermes BWS project; projected only when SSH automation needs it."
    }
  ]
}
